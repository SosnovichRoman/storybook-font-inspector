export const ADDON_ID = "font-inspector";
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = `font-inspector`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};
